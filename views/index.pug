extends layout
block content
  h1= title
  -//find the title in bookController.js for some reason
  p Welcome to #[em !{title}], a very basic Express website developed as a tutorial example on the Mozilla Developer Network.
  h1 Dynamic content
  h2 Log in here
  

  form(method='POST' action='')
    div.form-group
      label(for='user') Admin:
      input#user.form-control(type='text' placeholder='Enter Username' name='username' required='true')
      input#user.form-control(type='text' placeholder='Enter Password' name='password' required='true')

  if error
    p Error getting dynamic content.
  else
    p The library has the following record counts:

    ul
      li #[strong Books:] !{data.book_count}
      li #[strong Copies:] !{data.book_instance_count}
      li #[strong Copies available:] !{data.book_instance_available_count} 
      li #[strong Authors:] !{data.author_count}
      li #[strong Genres:] !{data.genre_count}

      li #[strong stuff:] !{title}